{
  "note": "The ## Audit Findings entry sits after the PM's plan and before any Executor entry. The main session runs the audit after execution and before it spawns the PM in ACCEPT mode, so the stage entry above the audit is an Executor entry.",
  "cites": [
    {
      "file": "skills/compute-squad/references/audit-prompts.md",
      "text": "Procedure for the main session on an audit-grade run, after execution and before spawning `squad-pm` in ACCEPT mode."
    },
    {
      "file": "skills/compute-squad/SKILL.md",
      "text": "The main session runs the fan-out after execution and before it spawns `squad-pm` in ACCEPT mode"
    }
  ],
  "lint": {
    "result": "fail",
    "rules": [
      "audit"
    ]
  },
  "resume": [
    {
      "state": {},
      "next": "perform Next: spawn squad-pm (ACCEPT) for WO-1",
      "via": "step 4: perform Next"
    }
  ]
}
